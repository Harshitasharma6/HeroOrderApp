import { createActions } from 'reduxsauce'


const { Types, Creators } = createActions({
    // Login action informations
    counterVisitAction: null,
    // influencerVisitAction: null,
    // siteVisitAction: null,
    // shreeCounterAction: null,
    // nonShreeCounterAction: null
})

export const CheckInTypes = Types
export default Creators